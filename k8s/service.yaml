apiVersion: v1
kind: Service
metadata:
  name: go-ghantt
  namespace: go-ghantt
  labels:
    app: go-ghantt
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: go-ghantt
